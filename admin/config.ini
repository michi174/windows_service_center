project_name = windows_service_center
project_display_name = Windows Service Center

controllername = site				; Kann per Post oder get übergeben werden. Bsp: index.php?site=news (Der Controller "news" muss geladen werden)
actionname = action					; Kann per Post oder Get übergeben werden. Bsp: index.php?site=news&action=new (Im Controller "news" wird die action "new" aufgerufen)
default_controller = index
controller_namespace = controller	; Wird vom Frontcontroller benötigt, damit die richtigen Controller geladen werden. Leer, wenn keine Namespaces verwendet werden.
subcontroller_namespace = subcontroller


database = wsc
database_user = Michael
database_password = 425262
database_host = localhost

default_time_zone = Europe/Vienna
default_link = site
default_action = action

project_dir = windows_service_center
plugin_dir = plugins
template_dir = template
class_dir = classes